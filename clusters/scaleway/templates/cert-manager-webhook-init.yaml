apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cert-manager-webhook-init
  namespace: argocd
  annotations:
    # Wave 3: after openbao-init (wave 2), whose chart declares the
    # ClusterSecretStore this app's ExternalSecret depends on. Same wave as
    # external-dns-init — both only need that ClusterSecretStore.
    argocd.argoproj.io/sync-wave: "3"
spec:
  project: default

  source:
    repoURL: {{ .Values.repoURL }}
    targetRevision: {{ .Values.revision }}
    path: apps/cert-manager-webhook-init

  destination:
    server: https://kubernetes.default.svc
    namespace: cert-manager

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
