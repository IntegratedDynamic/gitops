replicas: 2

image:
  repository: nginx
  tag: "1.27"

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 250m
    memory: 256Mi

ingress:
  enabled: true
  className: nginx
  host: demo.example.com  # override per environment
