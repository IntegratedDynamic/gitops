# OS / editor
.DS_Store
Thumbs.db
*.swp
*.swo
.vscode/
.idea/

# Secrets (never commit)
*.secret.yaml
*.secret.yml
kubeconfig*
*.key
*.pem
secrets/
# ...except the platform "secrets" sub-domain, which holds ArgoCD Application
# manifests (openbao / external-secrets), not actual secret material.
!platform/local/secrets/
!platform/scaleway/secrets/

# Helm
*.tgz

# Temp
tmp/
