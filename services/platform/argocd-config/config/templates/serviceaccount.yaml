{{- if .Values.enabled }}
# Identity for the PostSync restart hook below — kept as a plain resource
# (not part of the hook itself) so it exists before the hook Job runs.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: argocd-config-restarter
  namespace: {{ .Values.namespace }}
{{- end }}
