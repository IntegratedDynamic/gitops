apiVersion: v2
name: argocd-config
description: Materializes ArgoCD's own OIDC client secret from OpenBao and bounces argocd-server once it lands (HTTPRoute moved to services/platform/argocd-config/gateway)
type: application
version: 0.1.0
