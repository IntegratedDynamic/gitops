apiVersion: v2
name: argocd-config-secret
description: >-
  Materializes ArgoCD's own OIDC client secret from OpenBao. Split out of
  services/platform/argocd-config/config (infra#84 follow-up) so every
  platform domain's ExternalSecret lives in one place (infrastructure repo's
  10-cluster/scaleway/platform-apps/values-eso-data.yaml) — see that file's
  own header comment for why.
type: application
version: 0.1.0
