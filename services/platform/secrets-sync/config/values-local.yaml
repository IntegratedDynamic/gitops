# Cluster local (minikube).
#
# Désactivé (2026-08-25) : scaleway est désormais l'autorité sur la synchro
# GitHub (voir values-scaleway.yaml) — local n'écrit plus rien, pour éviter
# que les deux clusters se réécrivent mutuellement les mêmes secrets GitHub
# toutes les heures. C'est ici que la V0 avait été validée le 2026-07-20
# (le cluster local poussait alors réellement vers GitHub), avant que
# scaleway ne devienne le cluster réel/permanent du repo.
targets: []
