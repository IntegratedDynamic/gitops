# No overrides needed — base values.yaml already targets this environment's
# only current use. Kept as an empty file so this chart follows the same
# applications/scaleway -> values-scaleway.yaml convention as every other
# secret chart, in case a divergence shows up later.
