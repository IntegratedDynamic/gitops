# Wave 5: its HTTPRoute needs gateway-config's wave-4 Gateway to exist first
# (same reasoning as demo, wave 5). Its ExternalSecret being this late
# (rather than wave 3 alongside the other *-init charts) is fine — dex (in
# the wave-0 vendor chart) just retries via selfHeal until the Secret shows
# up, same tolerated race as external-dns.
name: dex-init
namespace: gateway
chartPath: services/platform/dex/init
valueFile: values.yaml
syncWave: "5"
